{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
                <div class="row col-md-11">
                    <div class="col-md-4">
                        <p>Trouvez la bonne affaire parmi <span class="count_annonces">{{ count_annonces }} petites annonces </span> sur leboncoin.</p><br/>
                        <a href="{{ path('ajouter_annonce') }}" class="round-btn fill-btn depot">Déposez gratuitement vos annonces</a><br/>
                        <a href="#" class="round-btn fill-btn recherche"> Rechercher autour de moi</a>
                    </div>
                    <div class="col-md-5">

                        <img src="{{ asset('images/france.svg') }}" alt="Example"/>

                    </div>
                    <div class="col-md-3">
                        <ul class="regions">

                            {% for r in regions %}
                                <li>{{ r.libelle }}</li>

                            {% endfor %}
                        </ul>
                    </div>
                </div>
{% endblock %}
